apiVersion: v1
kind: Service
metadata:
  name: inventory-grpc
spec:
  selector:
    app: inventory-service
  ports:
    - protocol: TCP
      port: 443
      name: grpc
      targetPort: 8081
  type: {{ .Values.service.type }}
