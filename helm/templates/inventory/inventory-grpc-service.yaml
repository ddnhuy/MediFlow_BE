apiVersion: v1
kind: Service
metadata:
  name: inventory-grpc
spec:
  selector:
    app: inventory-service
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      name: grpc
      targetPort: 8080
  type: {{ .Values.service.type }}
