apiVersion: apps/v1
kind: Deployment
metadata:
  name: management-service
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      app: management-service
  {{- with .Values.strategy }}
  strategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  template:
    metadata:
      labels:
        app: management-service
    spec:
      containers:
        - name: management-service
          image: "ddnhuy/management-service:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8080
          {{- with .Values.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          envFrom:
            - configMapRef:
                name: management-config
          volumeMounts:
            - name: config-volume
              mountPath: /app/appsettings.json
              subPath: appsettings.json
      initContainers:
        - name: wait-for-human-resource-grpc
          image: busybox
          command: ['sh', '-c', "until nc -z human-resource-grpc {{ .Values.service.port }}; do echo waiting for human-resource-grpc; sleep 2; done"]
        - name: wait-for-inventory-service
          image: busybox
          command: ['sh', '-c', "until nc -z inventory-service {{ .Values.service.port }}; do echo waiting for inventory-service; sleep 2; done"]
        - name: wait-for-vaccination-reception-service
          image: busybox
          command: ['sh', '-c', "until nc -z vaccination-reception-service {{ .Values.service.port }}; do echo waiting for vaccination-reception-service; sleep 2; done"]
      volumes:
        - name: config-volume
          configMap:
            name: management-config
            items:
              - key: appsettings.json
                path: appsettings.json