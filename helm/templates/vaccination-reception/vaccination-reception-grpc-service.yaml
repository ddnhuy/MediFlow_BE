apiVersion: v1
kind: Service
metadata:
  name: vaccination-reception-grpc
spec:
  selector:
    app: vaccination-reception-service
  ports:
    - protocol: TCP
      port: 443
      name: grpc
      targetPort: 8081
  type: {{ .Values.service.type }}
