apiVersion: v1
kind: Service
metadata:
  name: vaccination-reception-grpc
spec:
  selector:
    app: vaccination-reception-service
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      name: grpc
      targetPort: 8080
  type: {{ .Values.service.type }}
